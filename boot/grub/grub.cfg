## @file grub.cfg
## @brief GRUB menu configuration for booting wigOSX.

## @brief Boot the default kernel entry without waiting at the menu.
set default=0
set timeout=0

## @brief Menu entry that loads the wigOSX Multiboot kernel image.
menuentry "wigOSX" {
  multiboot /boot/wigOSX.bin
  boot
}
